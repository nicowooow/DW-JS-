let height= parseInt(window.getComputedStyle(ball).getPropertyValue("top"));
let width =  parseInt(window.getComputedStyle(ball).getPropertyValue("left"));

let ball = document.querySelector('.ball');

